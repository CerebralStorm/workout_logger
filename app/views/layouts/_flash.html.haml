- flash.each do |type, message|
  -if message.present?
    %div{:class => "alert #{bootstrap_class_for(type)} alert-dismissible", role: "alert"}
      %button.close{"data-dismiss" => "alert"} ×
      = message
    - flash[type.to_sym] = nil
